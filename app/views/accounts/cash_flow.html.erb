<h1>Fluxo de Caixa</h1>
<table>
	<tr>
		<th>Cliente:</th>
		<th>Fornecedor:</th>
    <th>Pago/Recebido:</th>
		<th>Data:</th>
	</tr>
	<tr>
		<td><%= select_tag :client_id, options_for_select(@clients, params[:client_id]), :prompt => 'Selecione' %></td>
		<td><%= select_tag :supplier_id, options_for_select(@suppliers, params[:supplier_id]), :prompt => 'Selecione' %></td>
    <td><%= select_tag :confirmed, options_for_select([[''],['sim'], ['não']], params[:confirmed]) %></td>

		<td><%= render :partial => '/shared/date_range' %></td>
	</tr>
</table>

<%= render :partial => 'cash_flow' , :locals => {:data => @sales + @payments} %>
